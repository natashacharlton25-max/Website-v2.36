{
  "component": "Toast",
  "category": "atoms/ui",
  "renders": { "base": "Toast.astro" },
  "notes": "Lottie icon plays 3 times max 10s. Static icon is Phosphor fallback shown in RM mode. Toast auto-dismisses after duration ms. TO mode strips all themes to plain dark bar and hides all icons.",
  "reducedMotion": "Slide/bounce/flip/zoom replaced with slow fade. Lottie hidden, static Phosphor icon shown.",
  "props": {
    "theme":      { "type": "string", "enum": ["arcade","professional","brutalist","glass","neon"], "default": "professional" },
    "icon":       { "type": "string", "enum": ["activity","notification","alert","info","thumbup"], "default": "notification" },
    "staticIcon": { "type": "string", "default": null },
    "animation":  { "type": "string", "enum": ["slide","bounce","fade","flip","zoom"], "default": "slide" },
    "message":    { "type": "string", "required": true },
    "duration":   { "type": "number", "default": 30000 }
  }
}
