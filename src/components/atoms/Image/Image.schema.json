{
  "component": "Image",
  "category": "atom",
  "renders": { "full": "Image.astro", "reduced": "Image.astro", "textonly": "Image.astro" },
  "notes": "Atomic image primitive — photos, illustrations, decorative SVGs, logos. Icon SVGs use Icon.astro. All 4 renders use the same Image.astro — pipeline strips animation props in reduced (no hover/tilt classes emitted). Textonly render keeps the image in the a11y tree but the alt text display system (data-alt-display-mode) controls visual presentation. Consumes theme tokens (--media-brightness, --media-saturation, --media-contrast) so monochrome/dark/high-contrast themes apply automatically. PIPELINE RULE: Images with AAC data (altAacHtml, altAacCards) are ALWAYS informational content — never stripped in any render mode. AAC cards are the accessible representation of the image. Decorative images (role=decorative) emit no HTML in textonly render — no CSS hide, just don't emit, so layout flows naturally with no gaps.",

  "props": {
    "content": {
      "_description": "What the image shows and how it's described",
      "src":            { "type": "string",  "required": true, "_format": "image-url", "description": "Image URL from CDN or local path." },
      "alt":            { "type": "string",  "required": false, "description": "Standard alt text for screen readers." },
      "role":           { "type": "string",  "required": false, "default": "content", "enum": ["content", "decorative"], "description": "Content wraps in <figure> with alt spans. Decorative renders bare <img alt='' aria-hidden>." },
      "altWord":        { "type": "string",  "required": false, "description": "Short word-level alt text (from API or manual)." },
      "altDescriptive": { "type": "string",  "required": false, "description": "Long descriptive alt text." },
      "altAacHtml":     { "type": "string",  "required": false, "description": "DEPRECATED — use altAacCards. Pre-rendered AAC card HTML." },
      "altAacCards":    { "type": "array",   "required": false, "description": "Array of AacCardData objects from load-alt-text.ts." },
      "semanticRole":   { "type": "string",  "required": true, "default": "decorative", "enum": ["decorative", "ui-control", "content-symbol"], "description": "Required. Declares intent — pipeline uses this to show/hide per render mode. Default: decorative (hidden in textonly/calm)." },
      "loading":        { "type": "string",  "required": false, "default": "lazy", "enum": ["lazy", "eager"], "description": "Loading strategy." }
    },

    "visual": {
      "_description": "How the image looks",
      "width":        { "type": ["number", "string"], "required": false, "description": "Image width." },
      "height":       { "type": ["number", "string"], "required": false, "description": "Image height." },
      "fit":          { "type": "string",  "required": false, "enum": ["cover", "contain", "fill", "none"], "description": "Object-fit mode." },
      "radius":       { "type": "string",  "required": false, "enum": ["none", "sm", "md", "lg", "full"], "description": "Border radius." },
      "shadow":       { "type": "string",  "required": false, "enum": ["none", "sm", "md", "lg", "inset"], "description": "Box shadow." },
      "border":       { "type": "string",  "required": false, "enum": ["none", "thin", "medium", "thick"], "description": "Border thickness. Colour from color variant." },
      "responsive":   { "type": "boolean", "required": false, "default": false, "description": "Fill container, override inline dimensions." },
      "sepia":        { "type": "string",  "required": false, "enum": ["light", "default", "heavy"], "description": "Warm vintage tone filter." },
      "blur":         { "type": "string",  "required": false, "enum": ["sm", "md", "lg", "xl"], "description": "Blur filter." },
      "gradientMask": { "type": "string",  "required": false, "enum": ["fade-bottom", "fade-top", "fade-edges", "vignette"], "description": "Soft fade mask." },
      "clipPath":     { "type": "string",  "required": false, "enum": ["circle", "ellipse", "diamond", "hexagon", "blob", "slant-left", "slant-right"], "description": "Shaped crop." },
      "maskIcon":     { "type": "string",  "required": false, "_format": "icon-slug", "description": "Phosphor icon slug for mask shape (fetched from Asset Library API)." },
      "maskShape":    { "type": "string",  "required": false, "_format": "shape-slug", "description": "Shape slug for mask (shape-001 to shape-072, fetched from Asset Library API)." },
      "bgFill":       { "type": "string",  "required": false, "enum": ["glass", "solid", "tint", "wash", "fill", "none"], "description": "Background fill for transparent PNGs." },
      "color":        { "type": "string",  "required": false, "default": "primary", "enum": ["primary", "secondary", "neutral", "red", "orange", "yellow", "teal", "blue", "purple", "pink"], "description": "Colour variant for border, caption, background." }
    },

    "animation": {
      "_description": "Motion behaviour — gated by .image--animate class (no prop = no class = no motion)",
      "hover": { "type": "boolean", "required": false, "default": false, "textonly": false, "description": "Scale + shadow on hover." },
      "tilt":  { "type": "string",  "required": false, "enum": ["sm", "md", "lg"], "textonly": false, "description": "Static tilt transform." }
    }
  }
}
