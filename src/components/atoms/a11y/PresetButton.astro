---
/**
 * PresetButton - Accessibility Preset Button Component
 *
 * A button for quick accessibility presets with icon and description.
 *
 * Usage:
 * <PresetButton
 *   preset="dyslexia"
 *   icon="text-aa-fill"
 *   title="Dyslexia"
 *   subtitle="Font + spacing"
 * />
 */
import Icon from '../icons/Icon.astro';

interface Props {
  preset: string;
  icon: string;
  title: string;
  subtitle: string;
  iconSize?: number;
}

const { preset, icon, title, subtitle, iconSize = 40 } = Astro.props;
---

<button
  class="a11y-preset-btn"
  data-preset={preset}
  aria-label={`${title} optimized settings`}
>
  <Icon name={`a11y-panel/${icon}`} size={iconSize} color="var(--brand-c-primary)" class="a11y-preset-btn__icon" />
  <h4 class="a11y-preset-btn__title">{title}</h4>
</button>

<style>
  .a11y-preset-btn {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
    width: 100%;
    padding: var(--space-xs) var(--space-lg);
    background: var(--brand-c-bg);
    border: 2px solid color-mix(in srgb, var(--brand-c-text-dark) 20%, transparent);
    border-radius: var(--radius-lg);
    cursor: pointer;
    transition: var(--transition-fast);
    text-align: left;
    box-shadow: 0 2px 8px color-mix(in oklch, var(--color-Black) 8%, transparent);
  }

  .a11y-preset-btn:hover {
    background: var(--brand-c-bg);
    border-color: var(--brand-c-primary);
    box-shadow: 0 4px 12px color-mix(in oklch, var(--color-Black) 12%, transparent);
  }

  .a11y-preset-btn:focus-visible {
    outline: 2px solid var(--brand-c-primary);
    outline-offset: 2px;
  }

  .a11y-preset-btn.active {
    background: var(--brand-c-bg);
    border-color: var(--brand-c-primary);
    box-shadow: 0 4px 12px color-mix(in oklch, var(--color-Black) 15%, transparent);
  }

  .a11y-preset-btn__icon {
    flex-shrink: 0;
    transition: var(--transition-fast);
  }

  .a11y-preset-btn__text {
    display: flex;
    flex-direction: column;
    gap: var(--space-3xs);
  }

  .a11y-preset-btn__title {
    font-weight: var(--font-bold);
    color: var(--brand-c-text-dark);
    text-transform: uppercase;
    letter-spacing: 0.02em;
  }

  .a11y-preset-btn__subtitle {
    color: var(--brand-c-text-dark);
    opacity: 0.7;
  }

  /* Tablet - stacked layout with subtitle */
  @media (min-width: 769px) and (max-width: 1400px) {
    .a11y-preset-btn {
      flex-direction: column;
      padding: var(--space-sm);
      gap: var(--space-xs);
      text-align: center;
    }

    .a11y-preset-btn__icon {
      width: 32px !important;
      height: 32px !important;
    }

    .a11y-preset-btn__text {
      align-items: center;
    }

    .a11y-preset-btn__title {
    }

    .a11y-preset-btn__subtitle {
    }
  }

  /* Mobile - smaller buttons, no subtitle */
  @media (max-width: 768px) {
    .a11y-preset-btn {
      flex-direction: column;
      padding: var(--space-xs) var(--space-sm);
      gap: var(--space-2xs);
      text-align: center;
    }

    .a11y-preset-btn__icon {
      width: 24px !important;
      height: 24px !important;
    }

    .a11y-preset-btn__text {
      align-items: center;
    }

    .a11y-preset-btn__title {
    }

    .a11y-preset-btn__subtitle {
      display: none;
    }
  }

  /* Small mobile - tighter */
  @media (max-width: 480px) {
    .a11y-preset-btn {
      padding: var(--space-xs);
    }

    .a11y-preset-btn__icon {
      width: 20px !important;
      height: 20px !important;
    }
  }

  /* Very small screens */
  @media (max-width: 320px) {
    .a11y-preset-btn {
      padding: var(--space-2xs);
      gap: var(--space-3xs);
    }

    .a11y-preset-btn__icon {
      width: 18px !important;
      height: 18px !important;
    }

    .a11y-preset-btn__title {
    }
  }
</style>
