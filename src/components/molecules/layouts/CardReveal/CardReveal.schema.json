{
  "component": "CardReveal",
  "category": "molecule",
  "renders": {
    "full": "CardReveal.astro",
    "reduced": "CardReveal.astro",
    "assistive": "CardReveal.astro",
    "textonly": "CardReveal.astro"
  },
  "notes": "Two-column card pair layout with click-to-reveal. Chrome only — Card atom wraps each slot, content is section organism's concern. Used standalone or as FlipCard textonly render target. Keyboard: Enter/Space toggles. Focus-within shows hover feedback.",

  "props": {
    "content": {
      "_description": "Layout config",
      "count": { "type": "number", "required": true, "description": "Number of card pairs. Determines slot count (front-0/back-0, front-1/back-1, etc)." }
    },
    "visual": {},
    "animation": {},
    "colour": {
      "_description": "Pipeline-overridable tokens",
      "activeBorder": { "type": "token", "required": false, "default": "brand-c-primary", "cssProperty": "--reveal-active-border", "description": "Active/selected card border colour." },
      "hoverBorder":  { "type": "token", "required": false, "default": "brand-c-secondary", "cssProperty": "--reveal-hover-border", "description": "Hover border colour." }
    }
  }
}
