{
  "component": "CardReveal",
  "category": "molecule",
  "renders": {
    "full": "CardReveal.astro",
    "reduced": "CardReveal.astro",
    "assistive": "CardReveal.astro",
    "textonly": "CardReveal.astro"
  },
  "notes": "Two-column master-detail card layout. Left: front cards (clickable). Right: companion detail cards (hidden until clicked). Click toggles independently. Used standalone or as FlipCard textonly render target. Card atom handles all chrome.",

  "props": {
    "content": {
      "_description": "Card pair data — maps from FlipCard content props",
      "items": {
        "type": "array",
        "required": true,
        "items": {
          "type": "object",
          "properties": {
            "title":     { "type": "string", "required": true },
            "subtitle":  { "type": "string", "required": false },
            "detail":    { "type": "string", "required": true },
            "href":      { "type": "string", "required": false },
            "linkLabel": { "type": "string", "required": false, "default": "Learn More" }
          }
        },
        "description": "Array of card pairs. FlipCard maps: title→title, subtitle→subtitle, description→detail, href→href, linkLabel→linkLabel."
      }
    },
    "visual": {},
    "animation": {},
    "colour": {}
  }
}
