---
interface Props {
  lead: string;
  subtitles: string[];
}

const { lead, subtitles } = Astro.props;
---

<section class="intro-text">
  <div class="container">
    <div class="intro-text__content">
      <p class="intro-text__lead">{lead}</p>
      {subtitles.map((subtitle) => (
        <p class="intro-text__subtitle">{subtitle}</p>
      ))}
    </div>
  </div>
</section>

<style>
  .intro-text {
    padding: var(--space-sm) 0;
  }

  .intro-text__content {
    max-width: 1400px;
    text-align: left;
  }

  .intro-text__lead {
    font-weight: var(--font-medium);
    color: var(--brand-c-text);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-xl);
  }

  .intro-text__subtitle {
    font-weight: var(--font-medium);
    color: var(--brand-c-text);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-sm);
  }

  .intro-text__subtitle:last-child {
    margin-bottom: 0;
  }

  @media (max-width: 767px) {
    .intro-text {
      padding: var(--space-2xl) 0;
    }
  }

  @media (max-width: 250px) {
    .intro-text__content {
      text-align: center;
    }

    .intro-text__lead,
    .intro-text__subtitle {
    }
  }

  @media (max-width: 210px) {
    .intro-text__lead,
    .intro-text__subtitle {
    }
  }
</style>
