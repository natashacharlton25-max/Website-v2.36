---
import BaseLayout from '../layouts/BaseLayout.astro';
import CheckoutLayout from '../layouts/CheckoutLayout.astro';
import Button from '../components/atoms/ui/Button/Button.astro';
import DownloadSummary from '../components/molecules/checkout/DownloadSummary.astro';
import Badge from '../components/atoms/ui/Badge/Badge.astro';
---

<BaseLayout
  title="Checkout | Walking with a Smile"
  description="Sign up to get your free downloads"
>
  <CheckoutLayout>
    <!-- Trust Badges -->
    <div slot="trust" style="display: flex; gap: var(--space-sm); justify-content: center; flex-wrap: wrap;">
      <Badge label="Secure & Private" icon="shield-check-fill" iconCollection="trust" uppercase={false} />
      <Badge label="Instant Delivery" icon="envelope-fill" iconCollection="trust" uppercase={false} />
      <Badge label="No Spam Promise" icon="seal-check-fill" iconCollection="trust" uppercase={false} />
    </div>

    <Fragment slot="form">
      <form id="newsletter-form" class="checkout-form">
        <div class="form-row">
          <div class="form-group">
            <label for="firstName" class="form-label">First Name *</label>
            <input
              type="text"
              id="firstName"
              name="firstName"
              class="form-input"
              placeholder="Your first name"
              required
            />
          </div>
          <div class="form-group">
            <label for="lastName" class="form-label">Last Name *</label>
            <input
              type="text"
              id="lastName"
              name="lastName"
              class="form-input"
              placeholder="Your last name"
              required
            />
          </div>
        </div>

        <div class="form-group">
          <label for="email" class="form-label">Email Address *</label>
          <input
            type="email"
            id="email"
            name="email"
            class="form-input"
            placeholder="you@example.com"
            required
          />
          <p class="form-hint">We'll send your download links to this email</p>
        </div>

        <div class="form-group">
          <label class="checkbox-label">
            <input type="checkbox" id="newsletter" name="newsletter" checked />
            <span>Yes, I'd like to receive updates and resources from Walking with a Smile</span>
          </label>
        </div>

        <!-- Submit Button -->
        <div class="checkout-actions">
          <Button type="submit" variant="primary" size="lg" class="btn-block" id="submit-button">
            <span id="button-text">Get My Downloads</span>
            <span id="button-spinner" style="display: none;">Processing...</span>
          </Button>
        </div>
      </form>

      <div class="checkout-footer">
        <div class="checkout-info">
          <span>All resources are free. No credit card required.</span>
        </div>

        <p class="checkout-terms">
          By submitting, you agree to our
          <a href="/legal/terms">Terms of Service</a> and
          <a href="/legal/privacy">Privacy Policy</a>
        </p>
      </div>
    </Fragment>

    <DownloadSummary slot="summary" />
  </CheckoutLayout>
</BaseLayout>

<script>
  import '../lib/cart/checkout-form';
</script>
