---
/**
 * Search Page - Full-width immersive search experience
 *
 * Dedicated search page with large typography, bold design,
 * and card grid results layout.
 */
import BaseLayout from '../Layouts/BaseLayout.astro';
import Icon from '../components/Icons/Icon.astro';
import Badge from '../components/Badge/Badge.astro';
import { getCollection } from 'astro:content';

// Deterministic string → hue (mirrors Badge.astro topicHue)
function topicHue(str: string): number {
  let hash = 0;
  for (const ch of str.toLowerCase()) hash = ch.charCodeAt(0) + ((hash << 5) - hash);
  return (((hash * 2654435079) % 360) + 360) % 360;
}

// Get all collections at build time
const allInsights = await getCollection('insights');
const allAssets = await getCollection('assets');
const allProjects = await getCollection('projects');
const allPresentations = await getCollection('presentations');

// Helper to get folder slug from entry.id
const getSlug = (id: string) => {
  const normalized = id.replace(/[\\/]index\.md$/, '');
  return normalized.split(/[\\/]/).pop() || id;
};

// Build searchable content array from all collections
const searchableContent = [
  // Insights collection
  ...allInsights.map(entry => ({
    title: entry.data.title,
    description: entry.data.description || '',
    url: `/insights/${getSlug(entry.id)}`,
    category: 'Insight',
    image: entry.data.cardImage?.src || null,
  })),
  // Assets collection
  ...allAssets.map(entry => ({
    title: entry.data.name,
    description: entry.data.description || '',
    url: `/assets/${getSlug(entry.id)}`,
    category: 'Resource',
    type: entry.data.type,
    image: entry.data.cardImage?.src || null,
  })),
  // Projects collection
  ...allProjects.map(entry => ({
    title: entry.data.title,
    description: entry.data.description || '',
    url: `/projects/${getSlug(entry.id)}`,
    category: 'Project',
    image: entry.data.cardImage?.src || null,
  })),
  // Presentations collection
  ...allPresentations.map(entry => ({
    title: entry.data.title,
    description: entry.data.description || '',
    url: `/presentations/${getSlug(entry.id)}`,
    category: 'Presentation',
    image: entry.data.cardImage?.src || null,
  })),
  // Static pages
  { title: 'About Us', description: 'Learn about Walking With A Smile and our mission.', url: '/about', category: 'Page', image: null },
  { title: 'Contact', description: 'Get in touch with our team.', url: '/contact', category: 'Page', image: null },
  { title: 'Tools & Resources', description: 'Browse our collection of resources.', url: '/assets', category: 'Page', image: null },
  { title: 'Insights & Articles', description: 'Thoughtful perspectives and guides.', url: '/insights', category: 'Page', image: null },
  { title: 'For Professionals', description: 'Resources for therapists and coaches.', url: '/services', category: 'Service', image: null },
  { title: 'Projects', description: 'Our ongoing and completed work.', url: '/projects', category: 'Page', image: null },
];

const pageTitle = 'Search';
const pageDescription = 'Search our resources, insights, tools, and more.';
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <main class="search-page">
    <!-- Hero Section -->
    <section class="search-hero">
      <div class="search-hero__content container">
        <h1 class="search-hero__title">
          Find what you're <span class="search-hero__highlight">looking for</span>
        </h1>
        <p class="search-hero__subtitle">
          Search our resources, insights, and tools
        </p>

        <!-- Search Input -->
        <div class="search-input-wrapper">
          <span class="search-input__icon">
            <Icon name="magnifying-glass-fill" size={28} />
          </span>
          <input
            type="text"
            id="searchInput"
            class="search-input"
            placeholder="Type to search..."
            autocomplete="off"
            autofocus
          />
          <button id="clearSearch" class="search-input__clear" aria-label="Clear search">
            <Icon name="x-circle-fill" size={20} />
          </button>
        </div>
      </div>
    </section>

    <!-- Quick Links Section -->
    <section class="quick-links" id="quickLinks">
      <div class="quick-links__container container">
        <div class="quick-links__grid">
          <a href="/assets" class="quick-link-card">
            <div class="quick-link-card__icon">
              <Icon name="package-fill" size={48} />
            </div>
            <h3 class="quick-link-card__title">Tools & Resources</h3>
            <p class="quick-link-card__desc">Practical resources for your journey</p>
          </a>
          <a href="/insights" class="quick-link-card">
            <div class="quick-link-card__icon">
              <Icon name="book-open-fill" size={48} />
            </div>
            <h3 class="quick-link-card__title">Insights & Articles</h3>
            <p class="quick-link-card__desc">Thoughtful perspectives and guides</p>
          </a>
          <a href="/services" class="quick-link-card">
            <div class="quick-link-card__icon">
              <Icon name="users-fill" size={48} />
            </div>
            <h3 class="quick-link-card__title">For Professionals</h3>
            <p class="quick-link-card__desc">Resources for therapists and coaches</p>
          </a>
          <a href="/projects" class="quick-link-card">
            <div class="quick-link-card__icon">
              <Icon name="stack-fill" size={48} />
            </div>
            <h3 class="quick-link-card__title">Projects</h3>
            <p class="quick-link-card__desc">Our ongoing and completed work</p>
          </a>
        </div>
      </div>
    </section>

    <!-- Pre-rendered Badge templates for client-side search results -->
    <div id="badge-templates" hidden>
      {['Insight', 'Resource', 'Project', 'Presentation', 'Page', 'Service'].map(cat => (
        <div data-badge-cat={cat.toLowerCase()} data-topic-hue={topicHue(cat)}>
          <Badge type={cat} shape="pill" variant="fill" size="sm" />
        </div>
      ))}
    </div>

    <!-- Search Results Section -->
    <section class="search-results" id="searchResults">
      <div class="search-results__container container">
        <div class="search-results__header">
          <h2 class="search-results__title">
            <span id="resultsCount">0</span> Results
          </h2>
          <p class="search-results__query">for <span id="searchQuery"></span></p>
        </div>

        <div class="search-results__grid" id="resultsList">
          <!-- Results populated by JavaScript -->
        </div>

        <!-- No Results -->
        <div class="no-results" id="noResults">
          <div class="no-results__icon">
            <Icon name="magnifying-glass-fill" size={64} />
          </div>
          <h3 class="no-results__title">No results found</h3>
          <p class="no-results__text">Try a different search term or browse our categories above</p>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>

<style is:global>
  /* ========== SEARCH PAGE ========== */
  .search-page {
    min-height: 100vh;
    background: var(--brand-c-bg);
  }

  /* ========== HERO SECTION ========== */
  .search-hero {
    background: linear-gradient(135deg, var(--brand-c-primary) 0%, var(--brand-c-primary-dark) 100%);
    padding: var(--space-4xl) var(--space-lg);
    padding-top: calc(var(--nav-height, 80px) + var(--space-4xl));
    margin-top: calc(-1 * var(--nav-height, 80px));
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .search-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    pointer-events: none;
  }

  .search-hero__content {
    position: relative;
    z-index: 1;
  }

  .search-hero__title {
    font-family: var(--font-heading);
    font-size: var(--text-5xl);
    font-weight: var(--font-bold);
    color: var(--color-White);
    margin: 0 0 var(--space-md) 0;
    line-height: var(--leading-tight);
  }

  .search-hero__highlight {
    color: var(--brand-c-neutral-light);
  }

  .search-hero__subtitle {
    font-family: var(--font-body);
    font-size: var(--text-xl);
    color: color-mix(in srgb, var(--color-White) 85%, transparent);
    margin: 0 0 var(--space-2xl) 0;
  }

  /* ========== SEARCH INPUT ========== */
  .search-input-wrapper {
    position: relative;
    max-width: var(--container-2xl);
    margin: 0 auto;
  }

  .search-input__icon {
    position: absolute;
    left: var(--space-lg);
    top: 50%;
    transform: translateY(-50%);
    color: var(--brand-c-primary);
    pointer-events: none;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .search-input {
    width: 100%;
    padding: var(--space-lg) var(--space-3xl);
    padding-left: calc(var(--space-lg) + var(--space-xl) + var(--space-md));
    font-family: var(--font-body);
    font-size: var(--text-lg);
    border: none;
    border-radius: var(--radius-full);
    background: var(--color-White);
    color: var(--brand-c-text);
    box-shadow: var(--shadow-xl);
    transition: all var(--transition-base);
    position: relative;
    z-index: 1;
  }

  .search-input:focus {
    outline: none;
    box-shadow: var(--shadow-2xl), 0 0 0 var(--space-xs) color-mix(in srgb, var(--color-White) 30%, transparent);
  }

  .search-input::placeholder {
    color: var(--brand-c-text-light);
  }

  .search-input__clear {
    position: absolute;
    right: var(--space-md);
    top: 50%;
    transform: translateY(-50%);
    background: transparent;
    border: none;
    color: var(--brand-c-text-light);
    cursor: pointer;
    padding: var(--space-xs);
    border-radius: var(--radius-full);
    transition: all var(--transition-fast);
    display: none;
    align-items: center;
    justify-content: center;
  }

  .search-input__clear:hover {
    background: var(--brand-c-neutral-light);
    color: var(--brand-c-text);
  }

  .search-input__clear.show {
    display: flex;
  }

  /* ========== QUICK LINKS ========== */
  .quick-links {
    padding: var(--space-2xl) var(--space-lg);
    background: var(--brand-c-bg);
  }

  .quick-links__container {
    /* Uses global .container class */
  }

  .quick-links__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-lg);
  }

  .quick-link-card {
    background: var(--color-White);
    border-radius: var(--radius-xl);
    padding: var(--space-xl);
    text-decoration: none;
    text-align: center;
    box-shadow: var(--shadow-md);
    transition: all var(--transition-base);
    border: var(--border-width-2) solid transparent;
  }

  .quick-link-card:hover {
    transform: translateY(calc(-1 * var(--space-xs)));
    box-shadow: var(--shadow-xl);
    border-color: var(--brand-c-primary-light);
  }

  .quick-link-card:hover .quick-link-card__icon {
    color: var(--brand-c-primary-dark);
    transform: scale(1.1);
  }

  .quick-link-card__icon {
    margin: 0 auto var(--space-md);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--brand-c-primary);
    transition: all var(--transition-base);
  }

  .quick-link-card__title {
    font-family: var(--font-heading);
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--brand-c-text);
    margin: 0 0 var(--space-xs) 0;
  }

  .quick-link-card__desc {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--brand-c-text-light);
    margin: 0;
  }

  /* ========== SEARCH RESULTS ========== */
  .search-results {
    padding: var(--space-4xl) var(--space-lg);
    background: var(--brand-c-neutral-light);
    display: none;
  }

  .search-results.show {
    display: block;
  }

  .search-results__container {
    /* Uses global .container class */
  }

  .search-results__header {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: var(--space-sm);
    margin-bottom: var(--space-2xl);
    padding-bottom: var(--space-lg);
    border-bottom: var(--border-width) solid var(--brand-c-neutral-light);
  }

  .search-results__title {
    font-family: var(--font-heading);
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    color: var(--brand-c-text);
    margin: 0;
  }

  .search-results__title span {
    color: var(--brand-c-primary-dark);
  }

  .search-results__query {
    font-family: var(--font-heading);
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    color: var(--brand-c-text);
    margin: 0;
  }

  .search-results__query span {
    color: var(--brand-c-primary-dark);
  }

  .search-results__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-lg);
  }

  @media (max-width: 1024px) {
    .search-results__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 768px) {
    .search-results__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .search-results__grid {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }
  }

  /* ========== NO RESULTS ========== */
  .no-results {
    text-align: center;
    padding: var(--space-4xl) var(--space-lg);
    display: none;
  }

  .no-results.show {
    display: block;
  }

  .no-results__icon {
    color: var(--brand-c-neutral);
    margin-bottom: var(--space-lg);
  }

  .no-results__title {
    font-family: var(--font-heading);
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
    color: var(--brand-c-text);
    margin: 0 0 var(--space-sm) 0;
  }

  .no-results__text {
    font-family: var(--font-body);
    font-size: var(--text-base);
    color: var(--brand-c-text-light);
    margin: 0;
  }

  /* ========== RESPONSIVE ========== */
  @media (max-width: 1024px) {
    .quick-links__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .search-hero {
      padding: var(--space-3xl) var(--space-md);
      padding-top: calc(var(--nav-height, 70px) + var(--space-3xl));
      margin-top: calc(-1 * var(--nav-height, 70px));
    }

    .search-hero__title {
      font-size: var(--text-4xl);
    }

    .search-hero__subtitle {
      font-size: var(--text-lg);
    }

    .search-input {
      font-size: var(--text-base);
      padding: var(--space-md) var(--space-2xl);
      padding-left: calc(var(--space-md) + var(--space-xl) + var(--space-sm));
    }

    .search-input__icon {
      left: var(--space-md);
    }

    .quick-links {
      padding: var(--space-2xl) var(--space-md);
    }

    .search-results {
      padding: var(--space-3xl) var(--space-md);
    }

    /* Center-align result cards on mobile */
    .search-results__grid .result-item {
      flex-direction: column;
      text-align: center;
      padding: var(--space-xl) var(--space-lg);
      gap: var(--space-lg);
    }

    .search-results__grid .result-item::before {
      left: 0;
      right: 0;
      top: 0;
      bottom: auto;
      width: 100%;
      height: calc(var(--space-xs) + var(--border-width-2));
      transform: scaleX(0);
      border-radius: 0 0 var(--radius-sm) var(--radius-sm);
    }

    .search-results__grid .result-item:hover::before {
      transform: scaleX(1);
    }

    .search-results__grid .result-item__icon {
      margin: 0 auto;
      width: var(--space-4xl);
      height: var(--space-4xl);
      min-width: var(--space-4xl);
    }

    .search-results__grid .result-item__icon svg {
      width: var(--space-2xl);
      height: var(--space-2xl);
    }

    .search-results__grid .result-item__main {
      text-align: center;
    }

    .search-results__grid .result-item__breadcrumb {
      justify-content: center;
    }

    .search-results__grid .result-item__title {
      font-size: var(--text-xl);
    }

    .search-results__grid .result-item__desc {
      font-size: var(--text-sm);
    }

    .search-results__grid .result-item__arrow {
      display: none;
    }
  }

  @media (max-width: 600px) {
    .search-hero {
      padding: var(--space-2xl) var(--space-sm);
      padding-top: calc(var(--nav-height, 60px) + var(--space-2xl));
      margin-top: calc(-1 * var(--nav-height, 60px));
    }

    .search-hero__title {
      font-size: var(--text-3xl);
    }

    .search-hero__subtitle {
      font-size: var(--text-base);
      margin-bottom: var(--space-xl);
    }

    .quick-links__grid {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }

    .quick-link-card {
      padding: var(--space-lg);
      display: flex;
      align-items: center;
      text-align: left;
      gap: var(--space-md);
    }

    .quick-link-card__icon {
      width: var(--space-2xl);
      height: var(--space-2xl);
      margin: 0;
      flex-shrink: 0;
    }

    .quick-link-card__title {
      font-size: var(--text-base);
    }

    .quick-link-card__desc {
      font-size: var(--text-xs);
    }

  }

  @media (max-width: 480px) {
    .search-hero {
      padding: var(--space-xl) var(--space-xs);
      padding-top: calc(var(--nav-height, 60px) + var(--space-xl));
      margin-top: calc(-1 * var(--nav-height, 60px));
    }

    .search-hero__title {
      font-size: var(--text-2xl);
    }

    .search-hero__subtitle {
      font-size: var(--text-sm);
    }

    .search-input {
      font-size: var(--text-sm);
      padding: var(--space-sm) var(--space-xl);
      padding-left: calc(var(--space-sm) + var(--space-xl) + var(--space-xs));
    }

    .search-input__icon {
      left: var(--space-sm);
    }

    .quick-links {
      padding: var(--space-xl) var(--space-xs);
    }

    .quick-link-card {
      padding: var(--space-md);
    }

    .quick-link-card__icon {
      width: var(--space-2xl);
      height: var(--space-2xl);
    }

    .search-results {
      padding: var(--space-2xl) var(--space-xs);
    }

    .search-results__title {
      font-size: var(--text-xl);
    }

    .search-results__grid .result-item {
      padding: var(--space-lg) var(--space-md);
      gap: var(--space-md);
      border-radius: var(--radius-xl);
      border-width: var(--border-width-2);
    }

    .search-results__grid .result-item__icon {
      width: var(--space-3xl);
      height: var(--space-3xl);
      min-width: var(--space-3xl);
      border-radius: var(--radius-lg);
    }

    .search-results__grid .result-item__icon svg {
      width: var(--space-xl);
      height: var(--space-xl);
    }

    .search-results__grid .result-item__title {
      font-size: var(--text-lg);
    }

    .search-results__grid .result-item__desc {
      font-size: var(--text-sm);
    }

    .search-results__grid .result-item__category {
      font-size: var(--text-xs);
      padding: var(--space-xs) var(--space-sm);
    }
  }

  @media (max-width: 400px) {
    .search-hero {
      padding: var(--space-lg) var(--space-xs);
      padding-top: calc(var(--nav-height, 60px) + var(--space-lg));
      margin-top: calc(-1 * var(--nav-height, 60px));
    }

    .search-hero__title {
      font-size: var(--text-xl);
    }

    .search-hero__subtitle {
      font-size: var(--text-xs);
    }

    .search-input {
      padding: var(--space-xs) var(--space-lg);
      padding-left: calc(var(--space-xs) + var(--space-lg) + var(--space-xs));
      border-radius: var(--radius-lg);
    }

    .search-input__icon {
      left: var(--space-xs);
    }

    .quick-links {
      padding: var(--space-lg) var(--space-xs);
    }

    .quick-link-card {
      padding: var(--space-sm);
      gap: var(--space-sm);
      border-radius: var(--radius-lg);
      flex-direction: column;
      text-align: center;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__icon {
      width: var(--space-2xl);
      height: var(--space-2xl);
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__title {
      font-size: var(--text-sm);
      text-align: center;
    }

    .quick-link-card__desc {
      display: none;
    }

    .search-results {
      padding: var(--space-xl) var(--space-xs);
    }

    .search-results__title {
      font-size: var(--text-lg);
    }

    .search-results__grid .result-item {
      padding: var(--space-md);
      border-radius: var(--radius-lg);
      gap: var(--space-sm);
    }

    .search-results__grid .result-item__icon {
      width: var(--space-2xl);
      height: var(--space-2xl);
      min-width: var(--space-2xl);
      border-radius: var(--radius-md);
    }

    .search-results__grid .result-item__icon svg {
      width: var(--space-lg);
      height: var(--space-lg);
    }

    .search-results__grid .result-item__title {
      font-size: var(--text-base);
      margin-bottom: var(--space-xs);
    }

    .search-results__grid .result-item__desc {
      font-size: var(--text-xs);
      -webkit-line-clamp: 1;
    }

    .search-results__grid .result-item__breadcrumb {
      font-size: var(--text-xs);
      margin-bottom: var(--space-xs);
    }

    .search-results__grid .result-item__category {
      font-size: var(--text-xs);
      padding: var(--space-xs) var(--space-sm);
    }

    .no-results__icon {
      transform: scale(0.75);
    }

    .no-results__title {
      font-size: var(--text-lg);
    }

    .no-results__text {
      font-size: var(--text-sm);
    }
  }

  @media (max-width: 350px) {
    .search-hero {
      padding: var(--space-md) var(--space-xs);
      padding-top: calc(var(--nav-height, 60px) + var(--space-md));
      margin-top: calc(-1 * var(--nav-height, 60px));
    }

    .search-hero__title {
      font-size: var(--text-lg);
    }

    .search-hero__subtitle {
      font-size: var(--text-xs);
      margin-bottom: var(--space-md);
    }

    .search-input {
      font-size: var(--text-xs);
      padding: var(--space-xs) var(--space-md);
      padding-left: calc(var(--space-xs) + var(--space-lg) + var(--space-xs));
    }

    .search-input__icon {
      left: var(--space-xs);
    }

    .quick-links {
      padding: var(--space-md) var(--space-xs);
    }

    .quick-link-card {
      padding: var(--space-xs);
      gap: var(--space-xs);
      border-radius: var(--radius-md);
      flex-direction: column;
      text-align: center;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__icon {
      width: var(--space-xl);
      height: var(--space-xl);
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__title {
      font-size: var(--text-xs);
      text-align: center;
    }

    .search-results {
      padding: var(--space-lg) var(--space-xs);
    }

    .search-results__title {
      font-size: var(--text-base);
    }

    .search-results__grid .result-item {
      padding: var(--space-xs) var(--space-sm);
      border-radius: var(--radius-sm);
      gap: var(--space-xs);
    }

    .search-results__grid .result-item__icon {
      width: var(--space-lg);
      height: var(--space-lg);
      border-radius: var(--radius-sm);
    }

    .search-results__grid .result-item__icon svg {
      width: var(--space-md);
      height: var(--space-md);
    }

    .search-results__grid .result-item__title {
      font-size: var(--text-xs);
      margin-bottom: var(--space-xs);
    }

    .search-results__grid .result-item__desc {
      display: none;
    }

    .search-results__grid .result-item__arrow {
      width: var(--space-md);
      height: var(--space-md);
    }
  }

  @media (max-width: 300px) {
    .search-hero {
      padding: var(--space-sm) var(--space-xs);
      padding-top: calc(var(--nav-height, 60px) + var(--space-sm));
      margin-top: calc(-1 * var(--nav-height, 60px));
    }

    .search-hero__title {
      font-size: var(--text-base);
    }

    .search-hero__subtitle {
      display: none;
    }

    .search-input {
      padding: var(--space-xs) var(--space-sm);
      padding-left: calc(var(--space-xs) + var(--space-md) + var(--space-xs));
      border-radius: var(--radius-md);
    }

    .search-input__icon {
      left: var(--space-xs);
    }

    .quick-links {
      padding: var(--space-md) var(--space-xs);
    }

    .quick-link-card {
      padding: var(--space-xs);
      border-radius: var(--radius-sm);
      flex-direction: column;
      text-align: center;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__icon {
      width: var(--space-xl);
      height: var(--space-xl);
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__title {
      font-size: var(--text-xs);
      text-align: center;
    }

    .search-results {
      padding: var(--space-md) var(--space-xs);
    }

    .search-results__title {
      font-size: var(--text-sm);
    }

    .search-results__grid .result-item {
      padding: var(--space-xs);
      border-radius: var(--radius-sm);
      gap: var(--space-xs);
    }

    .search-results__grid .result-item__icon {
      display: none;
    }

    .search-results__grid .result-item__breadcrumb {
      font-size: var(--text-xs);
    }

    .search-results__grid .result-item__title {
      font-size: var(--text-xs);
    }

    .search-results__grid .result-item__arrow {
      display: none;
    }
  }

  @media (max-width: 200px) {
    .search-hero {
      padding: var(--space-xs) 0;
      padding-top: calc(var(--nav-height, 50px) + var(--space-xs));
      margin-top: calc(-1 * var(--nav-height, 50px));
    }

    .search-hero__title {
      font-size: var(--text-sm);
    }

    .search-input {
      padding: var(--space-xs) var(--space-xs);
      padding-left: calc(var(--space-xs) + var(--space-md) + var(--space-xs));
      font-size: var(--text-xs);
      border-radius: var(--radius-sm);
    }

    .search-input__icon {
      left: var(--space-xs);
    }

    .quick-links {
      padding: var(--space-xs) 0;
    }

    .quick-link-card {
      padding: var(--space-xs);
      gap: var(--space-xs);
      flex-direction: column;
      text-align: center;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__icon {
      width: var(--space-lg);
      height: var(--space-lg);
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__title {
      font-size: var(--text-xs);
      text-align: center;
    }

    .search-results {
      padding: var(--space-sm) 0;
    }

    .search-results__header {
      margin-bottom: var(--space-sm);
    }

    .search-results__title {
      font-size: var(--text-xs);
    }

    .search-results__query {
      font-size: var(--text-xs);
    }

    .search-results__grid .result-item {
      padding: var(--space-xs);
      gap: var(--space-xs);
    }

    .search-results__grid .result-item__icon {
      display: none;
    }

    .search-results__grid .result-item__breadcrumb {
      font-size: var(--text-xs);
      gap: var(--space-xs);
    }

    .search-results__grid .result-item__title {
      font-size: var(--text-xs);
      margin-bottom: var(--space-xs);
    }

    .no-results {
      padding: var(--space-md);
    }

    .no-results__icon {
      transform: scale(0.5);
    }

    .no-results__title {
      font-size: var(--text-sm);
    }

    .no-results__text {
      font-size: var(--text-xs);
    }
  }

  @media (max-width: 100px) {
    .search-hero {
      padding: var(--space-xs) 0;
      padding-top: calc(var(--nav-height, 40px) + var(--space-xs));
      margin-top: calc(-1 * var(--nav-height, 40px));
    }

    .search-hero__title {
      font-size: var(--text-xs);
    }

    .search-input {
      padding: var(--space-xs) var(--space-xs);
      padding-left: calc(var(--space-xs) + var(--space-md) + var(--space-xs));
      font-size: var(--text-xs);
    }

    .search-input__icon {
      left: var(--space-xs);
    }

    .quick-links {
      padding: var(--space-xs) 0;
    }

    .quick-link-card {
      padding: var(--space-xs);
      gap: var(--space-xs);
      border-radius: var(--radius-sm);
      flex-direction: column;
      text-align: center;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__icon {
      width: var(--space-md);
      height: var(--space-md);
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .quick-link-card__title {
      font-size: var(--text-xs);
      text-align: center;
    }

    .search-results {
      padding: var(--space-xs) 0;
    }

    .search-results__title {
      font-size: var(--text-xs);
    }

    .search-results__query {
      font-size: var(--text-xs);
    }

    .search-results__grid {
      gap: var(--space-xs);
    }

    .search-results__grid .result-item {
      padding: var(--space-xs);
      border-radius: var(--radius-sm);
      gap: var(--space-xs);
    }

    .search-results__grid .result-item__icon {
      display: none;
    }

    .search-results__grid .result-item__breadcrumb {
      font-size: var(--text-xs);
      gap: var(--space-xs);
    }

    .search-results__grid .result-item__title {
      font-size: var(--text-xs);
      margin-bottom: var(--space-xs);
    }

    .no-results {
      padding: var(--space-xs);
    }

    .no-results__icon {
      transform: scale(0.3);
      margin-bottom: var(--space-xs);
    }

    .no-results__title {
      font-size: var(--text-xs);
    }

    .no-results__text {
      font-size: var(--text-xs);
    }
  }

  /* ========== REDUCED MOTION ========== */
  /* Quick link cards - no visual change on hover */
  #a11y-content-wrapper.a11y-reduce-motion .quick-link-card {
    transition: none !important;
  }

  #a11y-content-wrapper.a11y-reduce-motion .quick-link-card:hover {
    transform: none !important;
    box-shadow: var(--shadow-md) !important;
    border-color: transparent !important;
  }

  #a11y-content-wrapper.a11y-reduce-motion .quick-link-card__icon {
    transition: none !important;
  }

  #a11y-content-wrapper.a11y-reduce-motion .quick-link-card:hover .quick-link-card__icon {
    color: var(--brand-c-primary) !important;
    transform: none !important;
  }

  /* Search input - no transitions */
  #a11y-content-wrapper.a11y-reduce-motion .search-input {
    transition: none !important;
  }

  #a11y-content-wrapper.a11y-reduce-motion .search-input__clear {
    transition: none !important;
  }

  @media (prefers-reduced-motion: reduce) {
    .quick-link-card {
      transition: none;
    }

    .quick-link-card:hover {
      transform: none;
      box-shadow: var(--shadow-md);
      border-color: transparent;
    }

    .quick-link-card__icon {
      transition: none;
    }

    .quick-link-card:hover .quick-link-card__icon {
      color: var(--brand-c-primary);
      transform: none;
    }

    .search-input {
      transition: none;
    }

    .search-input__clear {
      transition: none;
    }
  }
</style>

<script is:inline define:vars={{ searchableContent: JSON.stringify(searchableContent) }}>
  // Search functionality - proper singleton with cleanup
  (function() {
    class SearchPage {
      constructor() {
        this.searchInput = document.getElementById('searchInput');
        this.clearBtn = document.getElementById('clearSearch');
        this.quickLinks = document.getElementById('quickLinks');
        this.searchResults = document.getElementById('searchResults');
        this.resultsList = document.getElementById('resultsList');
        this.noResults = document.getElementById('noResults');
        this.resultsCount = document.getElementById('resultsCount');
        this.searchQuery = document.getElementById('searchQuery');

        this.content = JSON.parse(searchableContent);

        // Build badge template map from pre-rendered Badge components
        this.badgeTemplates = {};
        this.topicHues = {};
        document.querySelectorAll('#badge-templates [data-badge-cat]').forEach(el => {
          this.badgeTemplates[el.dataset.badgeCat] = el.innerHTML;
          this.topicHues[el.dataset.badgeCat] = el.dataset.topicHue;
        });

        // Bind handlers so we can remove them later
        this.handleInputBound = this.handleInput.bind(this);
        this.handleClearBound = this.handleClear.bind(this);

        this.init();
      }

      destroy() {
        this.searchInput?.removeEventListener('input', this.handleInputBound);
        this.clearBtn?.removeEventListener('click', this.handleClearBound);
      }

      init() {
        // Search input handler
        this.searchInput?.addEventListener('input', this.handleInputBound);

        // Clear button
        this.clearBtn?.addEventListener('click', this.handleClearBound);

        // Check URL for search query
        const urlParams = new URLSearchParams(window.location.search);
        const query = urlParams.get('q');
        if (query && this.searchInput) {
          this.searchInput.value = query;
          this.handleSearch(query);
        }
      }

      handleInput(e) {
        this.handleSearch(e.target.value);
      }

      handleClear() {
        if (this.searchInput) {
          this.searchInput.value = '';
          this.searchInput.focus();
          this.handleSearch('');
        }
      }

      handleSearch(query) {
        const trimmedQuery = query.trim().toLowerCase();

        // Show/hide clear button
        if (this.clearBtn) {
          this.clearBtn.classList.toggle('show', !!trimmedQuery);
        }

        if (!trimmedQuery) {
          // Show quick links, hide results
          if (this.quickLinks) this.quickLinks.style.display = 'block';
          if (this.searchResults) this.searchResults.classList.remove('show');
          return;
        }

        // Hide quick links, show results section
        if (this.quickLinks) this.quickLinks.style.display = 'none';
        if (this.searchResults) this.searchResults.classList.add('show');

        // Update query display
        if (this.searchQuery) this.searchQuery.textContent = query;

        // Filter content
        const results = this.content.filter(item =>
          item.title.toLowerCase().includes(trimmedQuery) ||
          item.description.toLowerCase().includes(trimmedQuery) ||
          item.category.toLowerCase().includes(trimmedQuery)
        );

        this.renderResults(results);
      }

      getIconForCategory(category) {
        const icons = {
          'Insight': '<svg viewBox="0 0 256 256" fill="currentColor"><path d="M176,232a8,8,0,0,1-8,8H88a8,8,0,0,1,0-16h80A8,8,0,0,1,176,232Zm40-128a87.55,87.55,0,0,1-33.64,69.21A16.24,16.24,0,0,0,176,186v6a16,16,0,0,1-16,16H96a16,16,0,0,1-16-16v-6a16,16,0,0,0-6.23-12.66A87.59,87.59,0,0,1,40,104.5C39.74,56.83,78.26,17.15,125.88,16A88,88,0,0,1,216,104Zm-16,0a72,72,0,0,0-73.74-72c-39,.92-70.47,33.39-70.26,72.39a71.64,71.64,0,0,0,27.64,56.3A32,32,0,0,1,96,186v6h24V147.31L90.34,117.66a8,8,0,0,1,11.32-11.32L128,132.69l26.34-26.35a8,8,0,0,1,11.32,11.32L136,147.31V192h24v-6a32.12,32.12,0,0,1,12.47-25.35A71.65,71.65,0,0,0,200,104Z"/></svg>',
          'Page': '<svg viewBox="0 0 256 256" fill="currentColor"><path d="M213.66,82.34l-56-56A8,8,0,0,0,152,24H56A16,16,0,0,0,40,40V216a16,16,0,0,0,16,16H200a16,16,0,0,0,16-16V88A8,8,0,0,0,213.66,82.34ZM160,51.31,188.69,80H160ZM200,216H56V40h88V88a8,8,0,0,0,8,8h48V216Z"/></svg>',
          'Resource': '<svg viewBox="0 0 256 256" fill="currentColor"><path d="M223.68,66.15,135.68,18a15.88,15.88,0,0,0-15.36,0l-88,48.17a16,16,0,0,0-8.32,14v95.64a16,16,0,0,0,8.32,14l88,48.17a15.88,15.88,0,0,0,15.36,0l88-48.17a16,16,0,0,0,8.32-14V80.18A16,16,0,0,0,223.68,66.15ZM128,120,47.65,76,128,32l80.35,44Z"/></svg>',
          'Service': '<svg viewBox="0 0 256 256" fill="currentColor"><path d="M117.25,157.92a60,60,0,1,0-66.5,0A95.83,95.83,0,0,0,3.53,195.63a8,8,0,1,0,13.4,8.74,80,80,0,0,1,134.14,0,8,8,0,0,0,13.4-8.74A95.83,95.83,0,0,0,117.25,157.92ZM40,108a44,44,0,1,1,44,44A44.05,44.05,0,0,1,40,108Zm210.14,98.7a8,8,0,0,1-11.07-2.33A79.83,79.83,0,0,0,172,168a8,8,0,0,1,0-16,44,44,0,1,0-16.34-84.87,8,8,0,1,1-5.94-14.85,60,60,0,0,1,55.53,105.64,95.83,95.83,0,0,1,47.22,37.71A8,8,0,0,1,250.14,206.7Z"/></svg>',
          'Project': '<svg viewBox="0 0 256 256" fill="currentColor"><path d="M200,32H56A24,24,0,0,0,32,56V200a24,24,0,0,0,24,24H200a24,24,0,0,0,24-24V56A24,24,0,0,0,200,32Zm8,168a8,8,0,0,1-8,8H56a8,8,0,0,1-8-8V104H208Zm0-112H48V56a8,8,0,0,1,8-8H200a8,8,0,0,1,8,8Z"/></svg>',
          'Presentation': '<svg viewBox="0 0 256 256" fill="currentColor"><path d="M216,40H136V24a8,8,0,0,0-16,0V40H40A16,16,0,0,0,24,56V176a16,16,0,0,0,16,16H79.36L57.75,219.12a8,8,0,1,0,12.5,10l29.59-37.12H156.16l29.59,37.12a8,8,0,1,0,12.5-10L176.64,192H216a16,16,0,0,0,16-16V56A16,16,0,0,0,216,40Zm0,136H40V56H216Z"/></svg>',
        };
        return icons[category] || icons['Page'];
      }

      renderResults(results) {
        if (!this.resultsList || !this.noResults || !this.resultsCount) return;

        this.resultsCount.textContent = results.length;

        if (results.length === 0) {
          this.resultsList.innerHTML = '';
          this.noResults.classList.add('show');
          return;
        }

        this.noResults.classList.remove('show');

        // Build HTML string with proper CSS classes
        let html = '';
        results.forEach(item => {
          const categorySlug = item.category.toLowerCase();
          const iconSvg = this.getIconForCategory(item.category);

          const badgeHtml = this.badgeTemplates[categorySlug] || `<span class="result-card__badge">${item.category}</span>`;
          const hue = this.topicHues[categorySlug] || '0';

          html += `
            <a href="${item.url}" class="result-card" style="--topic-hue: ${hue}">
              <div class="result-card__badge-wrap">${badgeHtml}</div>
              <div class="result-card__icon">
                ${iconSvg}
              </div>
              <h3 class="result-card__title">${item.title}</h3>
              <p class="result-card__desc">${item.description}</p>
            </a>
          `;
        });

        this.resultsList.innerHTML = html;
      }
    }

    // Initialize with proper cleanup
    function initSearch() {
      if (!document.getElementById('searchInput')) return;

      // Destroy previous instance if exists
      if (window.__searchPageInstance) {
        window.__searchPageInstance.destroy();
      }

      // Create new instance
      window.__searchPageInstance = new SearchPage();
    }

    // Run on page load
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initSearch);
    } else {
      initSearch();
    }

    // For Astro page transitions
    document.addEventListener('astro:page-load', initSearch);
  })();
</script>
