---
/**
 * Visual Test Page - Display unused/experimental components
 *
 * Access at: /visualtest
 * Shows components that aren't currently in production but are available for use
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import StepCard from '../components/molecules/cards/StepCard.astro';
import CompactToolCard from '../components/molecules/cards/CompactToolCard.astro';
import Footer from '../components/organisms/nav/Footer/Footer.astro';

// Import sample images for CompactToolCard
import offering1 from '../PageImages/Services/offering-1-resources.png';
import offering2 from '../PageImages/Services/offering-2-whitelabel.png';
import offering3 from '../PageImages/Services/offering-3-custom.png';

// Sample data for StepCard
const stepCardData = [
  {
    number: '1',
    title: 'Initial Consultation',
    text: 'We start by understanding your needs, goals, and the people you work with.'
  },
  {
    number: '2',
    title: 'Resource Selection',
    text: 'Together we identify the most suitable resources and materials for your context.'
  },
  {
    number: '3',
    title: 'Implementation Support',
    text: 'We provide guidance and training to help you integrate the resources effectively.'
  }
];

// Sample data for CompactToolCard
const compactToolData = [
  {
    href: '#',
    name: 'Mindfulness Guide',
    category: 'Wellbeing',
    description: 'A comprehensive guide to mindfulness practices for daily life and emotional regulation.',
    image: offering1
  },
  {
    href: '#',
    name: 'Communication Toolkit',
    category: 'Skills',
    description: 'Practical tools and techniques for effective communication in professional settings.',
    image: offering2
  },
  {
    href: '#',
    name: 'Trauma-Informed Practice',
    category: 'Training',
    description: 'Essential frameworks and approaches for working with trauma-informed principles.',
    image: offering3
  }
];
---

<BaseLayout
  title="Visual Test Page | Component Library"
  description="Visual testing page for unused and experimental components"
  hideFooter={true}
>
  <div class="visualtest-page">
    <div class="container container-6xl">
      <header class="visualtest-header">
        <h1>Visual Component Library</h1>
        <p class="visualtest-subtitle">Testing page for unused and experimental components</p>
      </header>

      <!-- StepCard Section -->
      <section class="visualtest-section">
        <div class="section-header">
          <h2>StepCard Component</h2>
          <p class="section-description">
            Numbered step cards with circular badges. Originally designed for services page,
            currently replaced by TimelineStepper component.
          </p>
          <p class="component-location">
            <code>src/components/Cards/StepCard.astro</code>
          </p>
        </div>
        <div class="step-card-grid">
          {stepCardData.map(step => (
            <StepCard
              number={step.number}
              title={step.title}
              text={step.text}
            />
          ))}
        </div>
      </section>

      <!-- CompactToolCard Section -->
      <section class="visualtest-section">
        <div class="section-header">
          <h2>CompactToolCard Component</h2>
          <p class="section-description">
            Horizontal cards with small image thumbnails, category labels, and arrow indicators.
            Designed for compact tool/resource listings.
          </p>
          <p class="component-location">
            <code>src/components/Cards/CompactToolCard.astro</code>
          </p>
        </div>
        <div class="compact-tool-grid">
          {compactToolData.map(tool => (
            <CompactToolCard
              href={tool.href}
              name={tool.name}
              category={tool.category}
              description={tool.description}
              image={tool.image}
            />
          ))}
        </div>
      </section>

      <!-- Toast Section -->
      <section class="visualtest-section">
        <div class="section-header">
          <h2>Toast Component</h2>
          <p class="section-description">
            Toast notifications via <code>showToast()</code> from <code>src/lib/ui/toast.ts</code>.
            Lottie animations, 5 themes, 5 animation styles. Click toast to dismiss.
          </p>
          <p class="component-location">
            <code>src/lib/ui/toast.ts</code> + <code>src/styles/components/toast.css</code>
          </p>
        </div>

        <h3 class="toast-group-title">Themes</h3>
        <div class="toast-btn-grid">
          <button class="toast-trigger" data-theme="arcade" data-animation="slide" data-message="Arcade theme — game on!">Arcade</button>
          <button class="toast-trigger" data-theme="professional" data-animation="slide" data-message="Professional theme — clean and polished">Professional</button>
          <button class="toast-trigger" data-theme="brutalist" data-animation="slide" data-message="Brutalist theme — raw and bold">Brutalist</button>
          <button class="toast-trigger" data-theme="glass" data-animation="slide" data-message="Glass theme — frosted elegance">Glass</button>
          <button class="toast-trigger" data-theme="neon" data-animation="slide" data-message="Neon theme — electric vibes">Neon</button>
        </div>

        <h3 class="toast-group-title">Animations</h3>
        <div class="toast-btn-grid">
          <button class="toast-trigger" data-theme="neon" data-animation="slide" data-message="Slide animation">Slide</button>
          <button class="toast-trigger" data-theme="neon" data-animation="bounce" data-message="Bounce animation">Bounce</button>
          <button class="toast-trigger" data-theme="neon" data-animation="fade" data-message="Fade animation">Fade</button>
          <button class="toast-trigger" data-theme="neon" data-animation="flip" data-message="Flip animation">Flip</button>
          <button class="toast-trigger" data-theme="neon" data-animation="zoom" data-message="Zoom animation">Zoom</button>
        </div>

        <h3 class="toast-group-title">Icon Types</h3>
        <div class="toast-btn-grid">
          <button class="toast-trigger" data-theme="professional" data-animation="slide" data-icon="alert" data-message="Preset: alert">Preset (alert)</button>
          <button class="toast-trigger" data-theme="professional" data-animation="slide" data-icon="thumbup" data-message="Preset: thumbup">Preset (thumbup)</button>
          <button class="toast-trigger" data-theme="professional" data-animation="slide" data-icon="/_Unused Images/9.jpg" data-message="Image icon from public">Image (JPG)</button>
        </div>
      </section>

      <footer class="visualtest-footer">
        <p>
          This page is for development and testing purposes only.
          <br>
          Components shown here are functional but not currently used in production.
        </p>
      </footer>
    </div>
  </div>

  <Footer />
</BaseLayout>

<style>
  .visualtest-page {
    padding: var(--space-3xl) 0;
    background: var(--brand-c-bg);
    min-height: 100vh;
  }

  .visualtest-header {
    text-align: center;
    margin-bottom: var(--space-4xl);
    padding-bottom: var(--space-2xl);
    border-bottom: 2px solid var(--brand-c-primary-light);
  }

  .visualtest-header h1 {
    font-family: var(--font-heading);
    font-size: var(--text-4xl);
    font-weight: var(--font-bold);
    color: var(--brand-c-text);
    margin: 0 0 var(--space-md) 0;
  }

  .visualtest-subtitle {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    color: var(--brand-c-text-light);
    margin: 0;
  }

  .visualtest-section {
    margin-bottom: var(--space-4xl);
    padding: var(--space-3xl);
    background: var(--color-White);
    border-radius: var(--radius-xl);
    box-shadow: 0 2px 8px color-mix(in oklch, var(--color-Black) 5%, transparent);
  }

  .section-header {
    margin-bottom: var(--space-2xl);
    padding-bottom: var(--space-lg);
    border-bottom: 1px solid var(--brand-c-neutral-light);
  }

  .section-header h2 {
    font-family: var(--font-heading);
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
    color: var(--brand-c-text);
    margin: 0 0 var(--space-sm) 0;
  }

  .section-description {
    font-family: var(--font-body);
    font-size: var(--text-base);
    color: var(--brand-c-text-light);
    line-height: var(--leading-relaxed);
    margin: 0 0 var(--space-sm) 0;
  }

  .component-location {
    font-family: var(--font-mono, 'Courier New', monospace);
    font-size: var(--text-sm);
    color: var(--brand-c-primary-dark);
    margin: 0;
  }

  .component-location code {
    background: color-mix(in oklch, var(--brand-c-primary-light) 20%, transparent);
    padding: var(--space-xs) var(--space-sm);
    border-radius: var(--radius-sm);
    font-size: var(--text-sm);
  }

  /* StepCard Grid */
  .step-card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-xl);
  }

  /* CompactToolCard Grid */
  .compact-tool-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  /* Toast Buttons */
  .toast-group-title {
    font-family: var(--font-heading);
    font-size: var(--text-lg);
    font-weight: var(--font-bold);
    color: var(--brand-c-text-light);
    margin: var(--space-xl) 0 var(--space-md) 0;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .toast-group-title:first-of-type {
    margin-top: 0;
  }

  .toast-btn-grid {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
  }

  .toast-trigger {
    padding: var(--space-sm) var(--space-lg);
    border: 1px solid var(--brand-c-neutral-light);
    border-radius: var(--radius-md, 6px);
    background: var(--brand-c-bg);
    color: var(--brand-c-text);
    font-family: var(--font-body);
    font-size: var(--text-sm);
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .toast-trigger:hover {
    background: var(--brand-c-primary);
    color: var(--brand-c-bg);
    border-color: var(--brand-c-primary);
  }

  .visualtest-footer {
    text-align: center;
    padding: var(--space-2xl) 0;
    margin-top: var(--space-3xl);
    border-top: 2px solid var(--brand-c-neutral-light);
  }

  .visualtest-footer p {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--brand-c-text-light);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .visualtest-page {
      padding: var(--space-2xl) 0;
    }

    .visualtest-header h1 {
      font-size: var(--text-3xl);
    }

    .visualtest-section {
      padding: var(--space-xl);
    }

    .step-card-grid {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }
  }

  @media (max-width: 480px) {
    .visualtest-header {
      margin-bottom: var(--space-2xl);
    }

    .visualtest-section {
      padding: var(--space-lg);
    }

    .section-header h2 {
      font-size: var(--text-xl);
    }
  }
</style>

<script>
  document.querySelectorAll('.toast-trigger').forEach(btn => {
    btn.addEventListener('click', () => {
      const el = btn as HTMLElement;
      if ((window as any).showToast) {
        const opts: Record<string, any> = {
          message: el.dataset.message,
          theme: el.dataset.theme,
          animation: el.dataset.animation,
        };
        if (el.dataset.icon) opts.icon = el.dataset.icon;
        (window as any).showToast(opts);
      }
    });
  });
</script>
