{
  "_doc": "Phrases pre-rendered to SVG path data at build time. Run `npm run build:text-paths` to regenerate src/lib/text-paths/generated.ts. Add new phrases here as needed; runtime cost is zero — only the build script touches opentype.js. Keys are referenced via shorthand (e.g. Burst's tracePath: 'phrase:welcome').",
  "fonts": {
    "default": "public/Fonts/crumbo-demo.regular.ttf",
    "display": "public/Fonts/sukar-webfont/Sukar Black.woff"
  },
  "phrases": [
    { "key": "welcome",   "text": "WELCOME",            "font": "default", "size": 200 },
    { "key": "hello",     "text": "Hello",              "font": "default", "size": 200 },
    { "key": "brand",     "text": "Walking with a Smile", "font": "display", "size": 300 }
  ]
}
