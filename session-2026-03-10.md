# Session Log — 2026-03-10

## Decisions

| # | Decision | Outcome |
|---|----------|---------|
| D1 | Bridge fallback pattern is canonical | `var(--card-bg, var(--brand-c-bg))` — token-to-token. Hex fallbacks banned. Architecture decisions doc (9 Mar) overrides older prop layer mapping doc. |
| D2 | Prop layer mapping doc updated | "Do NOT add fallbacks" line replaced with bridge pattern rule + examples. |
| D3 | FormField variant flattening fixed | Previous session mechanically replaced all `--brand-c-*` with `--field-focus` / `--brand-c-primary` — lost secondary and neutral variant wiring. Restored correct mappings. |
| D4 | High contrast is zone-only, not a component prop | `contrast` prop removed from FormField. Zone file `high-contrast.css` already has `[data-high-contrast] .form-field` rules. A11y panel button sets the data attribute globally. |
| D5 | Dark mode FormField selectors migrated | Legacy `:is(.a11y-theme-dark, .a11y-theme-high-contrast)` → `[data-mode="dark"]` in theme-luminance-dark.css. |
| D6 | Prop layer bridge needs dark/light tiers | `--field-focus` alone is insufficient — need `--field-focus-dark` and `--field-focus-light` so JSON can override each tier independently. |
| D7 | FormField gets `colour` group in schema | Matches Badge, Button, Card pattern. Pipeline sets `--field-focus`, `--field-bg`, etc. as inline styles. `"default": "auto"` = pipeline resolves from variant. |
| D8 | Liquid glass tokens are global | `--liquid-*` tokens live in `shadows.css` `:root`, not hardcoded per-component. Card consumes via `var(--liquid-*)`. Any component can reuse. |
| D9 | Liquid glass dark overrides are global | `[data-mode="dark"] { --liquid-inner-glow: ... }` at `:root` level, not card-scoped. All liquid glass consumers inherit dark mode automatically. |
| D10 | Pipeline variant class lifecycle confirmed | CSS variant classes (primary/secondary) are permanent structural CSS. Pipeline colours layer on top via inline `--field-focus: var(--token)`. Bridge fallbacks become dead code after pipeline ships. |
| D11 | Internal token routing consolidated | All consumption points use `--_field-*` internal tokens, not raw bridge chains. Base `.form-field` block is the single place where bridge → internal happens. |
| D12 | Heading `color` prop enum → semantic slots | Old enum `[primary, primary-dark, secondary, text, text-light, neutral, inherit]` replaced with `[accent, text, muted, inherit]` — the 4 CSS classes that actually exist. Pipeline `colour` group handles fine-grained colour values. `color` prop is a quick semantic slot picker. |
| D13 | Dark mode headings use primary-600 | In dark mode, `--_heading-color` overridden to `var(--primary-600)` in zone file. Prevents text-coloured headings (h1/h3/h4/h5) from becoming white-on-dark and blending into body text. Accent headings (h2/h6) unaffected — already use `--_heading-accent` which resolves to brand primary. |
| D14 | Icon `color` stays raw CSS prop, pipeline gets `--icon-color` | `color` prop is Astro-level escape hatch (any CSS value). `--icon-color` in `colour` group is pipeline channel. Same split as Heading. |
| D15 | Icon shadow/glow tokens differentiated | `--svg-drop-shadow` / `-md` / `-lg` and `--svg-glow-sm` / `-md` / `-lg` / `-secondary` / `-white` all exist in `images.css`. Icon CSS was routing all sizes to `-md` — fixed to match. |
| D16 | Image `decorative` prop stays out of schema | Deprecated alias for `role="decorative"`. Astro keeps for backwards compat, schema is pipeline contract — pipeline uses `role`. |
| D17 | Image internal tokens are lightweight | Only 3 bridge-style refs exist (border, caption, overlay). Rest of `--img-*` tokens are set in `tokens/images.css` and consumed directly — no bridge pattern. |
| D18 | Link `color` enum → semantic slots | Old enum `[primary, primary-dark, secondary, text, text-light, inherit]` → `[primary, accent, text, muted, inherit]` matching CSS classes. Same pattern as Heading. |
| D19 | `--text-color` is phantom token | Ghost variant and textSlide referenced `var(--text-color)` which doesn't exist. Actual body text token is `--text-body`. Routed through `--_link-text: var(--link-text, var(--text-body))`. |
| D20 | Link decorative px values accepted | `3px` underline-offset, `2px` decoration-thickness, `8px` highlight height, `1px` border padding — inline text decoration values that don't scale with container. Same reasoning as FormField tick positioning. |
| D21 | `.link--default` CSS redundant, class kept | Default variant inherits base `.link` styles. CSS rules deleted. Class still emitted because `highlight-links.css` targets `.link--default`. |

## Fixes Applied

### FormField — variant colour restoration
| Variant | Token | Was (broken) | Now (correct) |
|---|---|---|---|
| Base | `--_field-brand-dark` | `var(--brand-c-primary)` | `var(--brand-c-primary-dark)` |
| Base | `--_field-brand-light` | `var(--brand-c-primary)` | `var(--brand-c-primary-light)` |
| Secondary | all 3 tokens | all `var(--brand-c-primary)` | `secondary` / `secondary-dark` / `secondary-light` |
| Neutral | all 3 tokens | all `var(--brand-c-primary)` | `neutral` / `neutral-dark` / `neutral-light` |
| High contrast | `--_field-border` | `var(--brand-c-neutral-light)` | `var(--brand-c-text-dark)` |

### FormField — functional bug
- `aria-labelledby={${id}-label}` on card-select group referenced nonexistent id. Added `id={${id}-label}` to the `<label>` element.

### FormField — tokenisation
- 6x `0.2s ease` → `var(--transition-fast)` (input, checkmark, radio, toggle track, toggle thumb, card-face)
- 2x `2px solid` in reduced render → `var(--border-width-2) solid`
- Dead `Button` import removed from Astro file

### FormField — contrast prop removal
- `contrast` prop removed from Props interface, destructuring, class list, schema
- `.form-field--high-contrast` CSS block removed from FormField.css (zone file handles it)
- Doc comments updated in both .astro and .css

### FormField — colour group added to schema
- New `colour` group with 10 tokens: focus, focusDark, focusLight, bg, text, label, border, placeholder, error, controlMark
- Matches Badge/Button/Card pattern for pipeline colour injection

### FormField — Pass 2: internal token consolidation
- 6 new internal tokens added to base `.form-field`: `--_field-bg`, `--_field-text`, `--_field-label`, `--_field-placeholder`, `--_field-error`, `--_field-control-mark`
- 3 glass internal tokens added: `--_field-glass-bg`, `--_field-glass-blur`, `--_field-glass-border`
- 1 shadow internal token added: `--_field-shadow`
- All raw bridge chains replaced with internal `--_field-*` tokens throughout CSS (disabled, filled, glass, neumorphic, toggle, error, card-face, textonly — every consumption point)
- Redundant `--_field-border` redefinition in reduced render block deleted
- `border-radius: 999px` → `var(--radius-full)` on toggle track
- `class:list` replaces manual `.filter(Boolean).join(' ')` in FormField.astro
- `fieldStyle` schema description updated with render mode behaviour notes
- Doc comment updated to list all 18 internal tokens

### theme-luminance-dark.css — full tokenisation
- Badge selectors migrated from `#a11y-content-wrapper:is(.a11y-theme-dark, .a11y-theme-high-contrast)` to `[data-mode="dark"]`
- Badge border widths tokenised to `var(--border-width-2)`
- Neumorphic `rgb(5, 5, 5)` → `var(--color-Black)`
- All `rgba()` → `color-mix(in oklch, var(--color-Black/White) N%, transparent)`
- Bare `white` → `var(--color-White)`
- Duplicated `color-mix()` expressions → existing glass tokens (`var(--glass-bg-frosted)`, `var(--glass-border-light)`)
- `1px` border width → `var(--border-width)`
- Final state: zero `rgba()`, zero bare CSS colour keywords

### Liquid glass globalisation
- `shadows.css`: added `--liquid-blur`, `--liquid-tint`, `--liquid-inner-glow`, `--liquid-outer-glow`, `--liquid-border`, `--liquid-inner-shadow`, `--liquid-outer-shadow` to `:root`
- `Card.css`: `.card--liquid` now consumes `var(--liquid-*)` tokens instead of hardcoded rgba values
- `theme-luminance-dark.css`: liquid glass overrides changed from `.card--liquid`-scoped to global `:root`-level `[data-mode="dark"]` overrides; HC dark overrides also globalised

### Heading — Pass 2: internal token consolidation + audit fixes
- 15 internal `--_heading-*` tokens added to base `.heading` block (color, accent, underline, badge-bg/alt/neutral, badge-text, highlight/alt/neutral, icon/alt/neutral/text, media-bg)
- All raw bridge chains replaced with `var(--_heading-*)` throughout CSS (level defaults, colour overrides, divider, underline, badge, highlight, icon, media)
- Schema `colour` group added: 8 pipeline tokens (headingColor, headingAccent, headingUnderline, headingBadgeBg, headingBadgeText, headingHighlight, headingIcon, headingMediaBg)
- Schema `color` prop enum fixed: `[primary, primary-dark, secondary, text, text-light, neutral, inherit]` → `[accent, text, muted, inherit]` — matches actual CSS classes
- Props interface updated in Heading.astro to match new enum
- `class:list` migration: `headingClasses` and `wrapperClasses` arrays migrated from `.filter(Boolean).join(' ')` to Astro `class:list` on `<div>` and `<Tag>` elements
- Dark mode heading override added to `theme-luminance-dark.css`: `[data-mode="dark"] .heading { --_heading-color: var(--heading-color, var(--primary-600)); }`
- Doc comment updated to list all 15 internal tokens

### Icon — Pass 2: schema + CSS + class:list
- Schema `category` fixed: `"atoms/icons"` → `"atom"`
- `semanticRole` added to schema `content` group (existed in Astro Props, was missing from schema)
- `colour` group added: 3 pipeline tokens (`iconColor`, `iconShadow`, `iconGlow`)
- Animation `_description` updated: removed stale "a11y reduce-motion" language → "gated by prop-driven classes"
- Schema `notes` updated with textonly behaviour
- Shadow CSS fixed: sm/md/lg all pointed to `--svg-drop-shadow-md` → now route to correct `-sm`/`-md`/`-lg` tokens
- Glow CSS fixed: all variants pointed to `--svg-glow-md` → now route to `--svg-glow-sm`/`-md`/`-lg`/`-secondary`/`-white`
- Internal `--_icon-color` token added to base `.icon` with bridge `var(--icon-color, inherit)`
- `effectClasses` migrated from `.filter(Boolean).join(' ')` to array, `<span>` uses `class:list`

### Image — Pass 2: schema + CSS + class arrays
- Schema restructured: `renders` moved to top, `notes` added with render behaviour, `_description` on all prop groups
- `class` prop added to `visual` group (existed in Astro, missing from schema)
- `textonly: false` flags added to animation props (hover, tilt)
- `colour` group added: 3 pipeline tokens (`imgBorder`, `imgCaption`, `imgOverlay`)
- `decorative` prop intentionally left out of schema (deprecated, pipeline uses `role="decorative"`)
- 3 internal tokens added to `.image` base: `--_image-border`, `--_image-caption`, `--_image-overlay`
- All raw bridge chains replaced with internal tokens throughout CSS (focus-visible, alt text spans, overlay, tooltip, XL text reflow, replace mode)
- Focus-visible `2px` → `var(--border-width-2)` (outline + outline-offset)
- Class arrays cleaned: `.filter(Boolean)` removed from `imgClasses` and `figureClasses` — `class:list` handles falsy values natively

### Link — Pass 2: internal tokens + schema + transitions
- 7 internal `--_link-*` tokens added to `.link` base: color, hover, focus, highlight, accent, text, fill-text
- All raw bridge chains replaced with internal tokens throughout CSS (variants, colour overrides, animations, render modes)
- `--text-color` phantom token fixed → `--text-body` via `--_link-text` internal token (ghost variant + textSlide)
- Schema `color` enum fixed: `[primary, primary-dark, secondary, text, text-light, inherit]` → `[primary, accent, text, muted, inherit]`
- `colour` group added: 7 pipeline tokens (linkColor, linkHover, linkFocus, linkHighlight, linkAccent, linkText, linkFillText)
- Schema `notes` updated with render mode behaviour
- Transitions tokenised: `0.35s ease` → `var(--transition-base)`, `0.3s ease-in-out` → `var(--transition-base)`. Cubic-bezier on underlineGrow kept (custom curve).
- `.link--default` redundant CSS rules deleted (base `.link` covers them). Class still emitted for `highlight-links.css` targeting.
- Astro `color` type updated to match new enum
- Class array cleaned: mixed ternary/falsy → consistent falsy `&&`, `.filter(Boolean)` removed

### Prop layer mapping doc
- Replaced "Do NOT add fallback values" with bridge pattern rule and examples

### theme-luminance-dark.css
- FormField selectors migrated from legacy `:is(.a11y-theme-dark, ...)` to `[data-mode="dark"]`
- Heading dark mode override added: text headings → primary-600

## Documented Exceptions (no action needed)
- Hardcoded control sizes (22px, 44px etc.) — geometry for drawing tick/dot
- `opacity: 0.6` on placeholder — no token exists
- Select arrow SVG `#6b6b6b` — can't use CSS vars in data URIs
- `24px` native controls in textonly — consistent across input types

## Files Modified

| File | Changes |
|------|---------|
| `atom-prop-layer-prompt.md` | Bridge fallback rule replaces "no fallbacks" |
| `src/components/atoms/FormField/FormField.css` | Pass 1: variant colours restored, transitions tokenised, border tokenised, HC block removed. Pass 2: 10 internal tokens added, all raw bridge chains replaced, glass/shadow tokens, toggle radius tokenised, redundant reduced token deleted |
| `src/components/atoms/FormField/FormField.astro` | aria-labelledby fix, Button import removed, contrast prop removed, `class:list` migration |
| `src/components/atoms/FormField/FormField.schema.json` | contrast prop removed, colour group added (10 tokens), fieldStyle description updated |
| `src/styles/zones/theme-luminance-dark.css` | FormField selectors migrated, badge selectors migrated, full tokenisation (all rgba → color-mix, bare keywords → tokens, neumorphic rgb → var), liquid glass dark overrides globalised |
| `src/styles/tokens/shadows.css` | Global `--liquid-*` tokens added to `:root` |
| `src/components/atoms/Card/Card.css` | Liquid glass variant updated to consume `var(--liquid-*)` global tokens |
| `src/components/atoms/Heading/Heading.css` | Pass 2: 15 internal `--_heading-*` tokens added, all raw bridge chains replaced, doc comment updated |
| `src/components/atoms/Heading/Heading.astro` | `color` prop type updated to `accent\|text\|muted\|inherit`, `class:list` migration on both paths |
| `src/components/atoms/Heading/Heading.schema.json` | `color` enum fixed, `colour` group added (8 pipeline tokens) |
| `src/components/atoms/Icon/Icon.schema.json` | `category` fixed (`atoms/icons` → `atom`), `semanticRole` added to content, `colour` group added (3 tokens), animation description updated |
| `src/components/atoms/Icon/Icon.css` | Shadow/glow routing fixed (all pointed to `-md`, now correct per size), `--_icon-color` internal token added |
| `src/components/atoms/Icon/Icon.astro` | `class:list` migration on `<span>` |
| `src/components/atoms/Image/Image.schema.json` | Restructured: renders moved to top, notes added, _description on groups, class prop added, textonly flags, colour group (3 tokens) |
| `src/components/atoms/Image/Image.css` | 3 internal tokens added (--_image-border, --_image-caption, --_image-overlay), all bridge chains replaced, focus-visible 2px → var(--border-width-2) |
| `src/components/atoms/Image/Image.astro` | .filter(Boolean) removed from imgClasses and figureClasses |
| `src/components/atoms/Link/Link.css` | 7 internal `--_link-*` tokens added, all bridge chains replaced, `--text-color` → `--text-body`, transitions tokenised, `.link--default` redundant rules deleted |
| `src/components/atoms/Link/Link.astro` | `color` type updated to `primary\|accent\|text\|muted\|inherit`, class array cleaned |
| `src/components/atoms/Link/Link.schema.json` | `color` enum fixed, `colour` group added (7 tokens), notes updated |

## Second-Pass Audit Status (post theme token restructure)

Verified current file state — work from platform session is in the files:

| Atom | Prop layer | Variants | Legacy selectors | Zone gating | Status |
|---|---|---|---|---|---|
| Badge | `--_badge-*` bridge ✓ | fill/outline/glass ✓ | None ✓ | N/A (no HC/dark rules needed) | CLEAN |
| Button | `--_btn-*` bridge ✓ | primary/secondary/neutral correct ✓ | None ✓ | Delegated to theme tokens ✓ | CLEAN |
| FormField | `--field-*` bridge ✓ | **Was flattened** — fixed this session | **Had `.form-field--high-contrast`** — removed | Dark migrated to `[data-mode]`, HC in zone file | FIXED |

Badge and Button survived the mechanical prop layer migration intact. FormField was the casualty — all three variant colours got flattened to primary. Now restored.

---

## Pattern: Zone-Gated vs Component-Prop

This session confirmed the rule for all atoms going forward:

| Concern | Where it lives | How it triggers |
|---|---|---|
| Colour variant (primary/secondary/neutral) | Component CSS | JSON prop → class |
| Style variant (outlined/glass/neumorphic) | Component CSS | JSON prop → class |
| High contrast | `src/styles/zones/high-contrast.css` | A11y panel → `[data-high-contrast]` on page |
| Dark mode | `src/styles/zones/theme-luminance-dark.css` | Theme switcher → `[data-mode="dark"]` on page |
| Render mode | Component CSS (scoped `[data-render="..."]`) | Pipeline → `[data-render]` on body |

Components don't have props for high-contrast or dark mode. They respond to zone data attributes set by the global UI.

---

## Rainbow CSS Status (confirmed this session)

| File | Status |
|------|--------|
| `src/styles/tokens/rainbow-default.css` | Done — 35 tokens (7 colours x 5 tints), ROYGBIV, dark flip |
| `src/styles/tokens/rainbow-protan.css` | Done — blue-yellow axis, gated `[data-cvd="protan/deutan"]`, dark flip |
| `src/styles/tokens/rainbow-tritan.css` | Done — red-green axis |
| `src/styles/zones/theme-chroma-mono.css` | Done — grey rainbow for mono themes |
| `src/styles/tokens/confetti.css` | Done — consuming `var(--rainbow-*)` tokens |

**Deferred:** Remove `computeRainbow()`, `RAINBOW_PALETTES`, `getRainbowPalette()`, `auditRainbowContrast()` from `src/utils/theme-engine.js` — static CSS files are now the source of truth, engine code is dead.
